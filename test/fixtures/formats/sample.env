# fixture: fake values only
API_HOST=api.example.test
export API_TOKEN='tok-FAKEFAKEFAKE'
QUOTED_MESSAGE="line one\nline two"
COMMENTED=plain-value # this trailing comment is stripped
EMPTY=
SPACED = padded
UNICODE_LABEL="café — 多言語"
